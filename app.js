const s = document.getElementById("toggle-chat-btn"), c = document.getElementById("ai-chat-window"), r = document.getElementById("close-chat-btn"), d = document.getElementById("chat-input-form"), a = document.getElementById("chat-input"), l = document.getElementById("chat-messages"); let m = null; if (window.Worker) { m = new Worker("ai-worker.js"); m.onmessage = (e) => { const { type: t, payload: o } = e.data; if ("REPLY" === t) { const e = document.createElement("div"); e.className = "bg-slate-100 text-slate-800 p-3 rounded-2xl max-w-[85%] self-start"; e.innerHTML = o; l.appendChild(e); l.scrollTop = l.scrollHeight; } }; } if (s && c) { s.addEventListener("click", () => { const e = c.classList.contains("scale-0"); if (e) { c.classList.remove("scale-0", "opacity-0"); c.classList.add("scale-100", "opacity-100"); } else { c.classList.remove("scale-100", "opacity-100"); c.classList.add("scale-0", "opacity-0"); } }); } if (r) r.addEventListener("click", () => { c.classList.remove("scale-100", "opacity-100"); c.classList.add("scale-0", "opacity-0"); }); if (d) d.addEventListener("submit", (e) => { e.preventDefault(); const t = a.value.trim(); if (!t) return; const o = document.createElement("div"); o.className = "bg-brand-600 text-white p-3 rounded-2xl max-w-[85%] self-end align-right"; o.textContent = t; l.appendChild(o); a.value = ""; l.scrollTop = l.scrollHeight; if (m) m.postMessage({ type: "CHAT", payload: t }); }); window.addEventListener("load", () => { const e = window.location.hash.substring(1), t = { preise: "preisModal", datenschutz: "datenschutzModal", agb: "agbModal", impressum: "impressumModal" }[e]; if (t) setTimeout(() => window.openModal(t), 200); }); })();
